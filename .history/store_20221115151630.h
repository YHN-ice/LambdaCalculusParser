#include "lambda.h"

/*lambda Table Entry*/
struct laTE{
    char *id;
    struct L *l;
};
extern struct LaTE *table;
extern int capacity;

void store_symbol(char *id, struct L *l){
    for(struct LaTE *)
}
